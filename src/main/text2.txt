by default your metro booking system runs in port 8080. write the docker cmd to run container and map it to port
2)yiu need to run multiple containers of mbs for testing one on :8081 and other :8082
3)your teamate pushed updtes to docker file on main branch how will you update your main repo and rebuild image with tag v2
4)you want to see the logs of the container named-flightbooking. cmd??
5)the mbs image is consuming too much apCE WRITE A CMD TO REMOVE UNUSED
6)you want to update the mbs image with a new version how do you remove the old container and reun the updated one on port 8085
7) the mbs is crashing at startup how do you enter the containers shell to debug the ptoblem
8)your manager asjed you to check which port the mbs is mapped to which cmd youll use
9)how do you stop a run ning container with ID cla2b3
answer in a single line 

1. `docker run -d -p 8080:8080 mbs`

2. `docker run -d -p 8081:8080 mbs`
   `docker run -d -p 8082:8080 mbs`

3. `git pull origin main && docker build -t mbs:v2 .`

4. `docker logs flightbooking`

5. `docker system prune -f`

6. `docker rm -f <old_container_id> && docker run -d -p 8085:8080 mbs:new_version`

7. `docker exec -it <container_id> /bin/sh` (or `/bin/bash`)

8. `docker port <container_id_or_name>`

9. `docker stop cla2b3`
